## 2026-08-21 - [Memoize Sorted Lists in React] **Learning:** Sorting large arrays inside component render bodies without memoization (like `sortJobsBySubmitted`) causes O(N log N) performance hits on unrelated prop/state changes. **Action:** Always wrap expensive list derivations like sorting or filtering in `useMemo` when the source data changes less frequently than the component re-renders.
