## 2026-08-20 - [Optimize TimeMap Interpolation Search] **Learning:** When replacing an O(N) loop with `bisect_right` using the `key` argument (Python 3.10+), be careful not to break early returns for exact boundary matches that might alter data formatting. Using list comprehensions to extract keys before bisecting defeats the purpose of the optimization by incurring O(N) memory and time overhead. **Action:** Always use the `key=lambda` parameter for  when dealing with object lists, and write precise boundary checks to ensure exact-match conditions are preserved.
## 2026-08-20 - [Optimize TimeMap Interpolation Search] **Learning:** When replacing an O(N) loop with bisect_right using the key argument (Python 3.10+), be careful not to break early returns for exact boundary matches that might alter data formatting. Using list comprehensions to extract keys before bisecting defeats the purpose of the optimization by incurring O(N) memory and time overhead. **Action:** Always use the key=lambda parameter for bisect when dealing with object lists, and write precise boundary checks to ensure exact-match conditions are preserved.
