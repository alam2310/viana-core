/**
 * REMOVED from the dashboard (Step 6.8 / I001, 2026-08-21).
 *
 * Live Monitor widget and the queue Monitor action are gone. Job-row click
 * opens job details; Live Crossings live in
 * `features/telemetry/job-details-panel.tsx`.
 *
 * PARKED (S20 / S24): do not import or mount `LiveProcessedVideo` /
 * `crossing-media-sync` until that work is un-parked — see
 * docs/steps/STABILIZATION_BACKLOG.md.
 */

export {};
